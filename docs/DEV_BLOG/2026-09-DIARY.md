# Development Diary - September 2026

## 2026-09-17: build and install the direct Ground View button

Source `63ae138` linked as ARM64 native; both Android debug flavors and the
signed XR package build passed with versionCode 10220 /
`1.2.20-xr-ground-button`. APK SHA-256:
`1faa24d4ac50f82184a3ed2170b9e4df87248349c7cf54420562813bbdc3a8bc`.
The existing production v3 certificate, package ID, ARM64 ABI and disabled
debug cheats verify. `adb install -r` on Quest 3 `2G0YC5ZG9609PY` returned
Success; Android reports 10220 with the earlier first-install date. Production
menu routing 340 and bilingual panel payload 20,765 host checks pass. The
button's physical placement, readable label and direct laser click await
worn-headset use; do not mark them accepted from automated checks alone.

## 2026-09-17: add direct Ground View entry beside UI

P25.2 adds a compact bilingual Ground View / Bodenansicht button immediately
below UI. Both follow the movable build-window transform; rendering and ray
hit testing share the same surface geometry. The click enters the existing
ground-placement state with the same trigger-release and game-input guards as
UI > View. It appears only in an eligible offline Skirmish. Compact Canvas
titles now fit the full localized label. Production menu routing (340) and
bilingual panel payload (20,765) host checks pass. Native/package build,
installation and worn-headset appearance/input checks remain pending at this
checkpoint.

## 2026-09-17: build and install P25.1 ground movement

Native ARM64 `z_generals`, both `assembleZhDebug` and `assembleXrDebug`, and
release-signed XR package build pass from source `ee0f497`. Focused observer
state/mapping 56, production interaction 148 and bilingual panel 20,761 host
checks pass. VersionCode 10219 / `1.2.19-xr-ground-movement` APK SHA-256 is
`878dd8ef2e9ea0fd6399648049f88d6b08187d03abce7ce7301cd95c4e97527c`.
Production v3 signing certificate, package ID, ARM64 ABI, no retail archives
and disabled debug cheats verified. Installed via `adb install -r` on Quest 3
`2G0YC5ZG9609PY`; Android reports 10219 and retained first-install date.
Worn-headset locomotion, collision and comfort tests remain open.

## 2026-09-17: add simple ground-view locomotion source

After the maintainer's positive first impression of the installed P25 view,
P25.1 adds physical left-stick gaze-relative movement and physical right-stick
smooth yaw (2 m/s, 75 degrees/s) in the observer only. World rotation pivots
about the tracked head; game simulation and tactical camera stay unchanged.
Each small step checks actual terrain height/slope, map margins, shroud and
drawables. The direct B/Y return and input-release guard remain. The hint and
fifth controller-help page document physical-stick assignment in German and
English, including left-handed gameplay. Focused host state/mapping (56),
interaction routing (148) and bilingual panel payload (20,761) checks pass.
An incremental Android build, signed APK and worn-headset locomotion/comfort
test are still pending at this diary checkpoint.

## 2026-09-17: install the ground-observer test on Quest 3

Quest 3 serial `2G0YC5ZG9609PY` connected via USB/ADB. The app was not running;
`adb install -r` of the signed 10218 prototype returned Success. Android now
reports package `com.generalsx.zerohour.xr`, versionName
`1.2.18-xr-ground-observer` and the same first-install date as the prior 10217
installation, so the update retained app data. The local APK digest and v3
production certificate were rechecked. No worn-headset play or visual/comfort
result has been reported yet. Draft PR #15 remains open for that test.

## 2026-09-17: verify the separate ground-observer test APK

Parent review and incremental ARM64 build completed at source `d955d29`.
XR release packaging and `assembleXrDebug` / `assembleZhDebug` pass locally.
Independent production host reruns pass: observer 38, interaction 145,
loading presenter 152 and bilingual panel payload 20,693 checks. The reviewed
loading transition clears the fade and keeps observer mapping/compositor state
consistent; regular callback registration does not cancel the mode.

The separately named 10218 / `1.2.18-xr-ground-observer` APK has SHA-256
`88d8e4e87ebedd28cb01712aecd322b2c518fd87bfd0a1b5c75cdd989887ba1e`.
Production v3 signing, package identity, ARM64-only ABI, non-debuggable manifest,
no retail archives and debug-cheats-off checks pass. The preserved 1.2.17
official APK still matches its release digest. Installation and physical
acceptance were pending at this packaging checkpoint; the installation is
recorded in the later entry above. Stereo quality, comfort and return-path
acceptance remain open.
No official release, main merge or multiplayer change is part of this work.

## 2026-09-17: implement the isolated ground-observer source prototype

Checkpoint `413eab1` on `codex/xr-ground-observer-prototype` adds the P25
offline-Skirmish-only View action, terrain/shroud/slope guarded placement,
fixed human-scale world-to-room mapping, bounded viewer-relative stereo
visibility/terrain coverage, opaque observer compositing, passive input,
bilingual armed/return cards and direct B/Y return. The existing tabletop
mapping, layout data and release branch are untouched. The production-order
loading-presenter regression catches accidental per-frame observer exit;
the host observer 38, mapping bridge 26, interaction 145, loading presenter
152, bilingual panel 20,693, menu 2,890, workspace 781, console 1,499,
endgame 33, scene 1,280, world 215, height 272,822, board 74, input 27
and loading 517 checks pass. The parent-owned cached ARM64 native build linked
`libmain.so`; signed test packaging remains pending at this diary checkpoint.
No headset use or performance claim.

## 2026-09-17: start the passive ground-observer prototype

The maintainer authorized a bounded offline Skirmish experiment and requested
GPT-5.6 Sol with high reasoning for implementation. PLAN-025 specifies ground
selection, a human-scale stereo observer camera, passive input, bounded
viewer-relative terrain/visibility and a direct return to the unchanged
tabletop. Start from `6af7abd` in an isolated branch; preserve the official
1.2.17 release. Prototype build, graphics quality and headset comfort remain
open; no multiplayer or follow-camera behavior is included.

## 2026-09-16: publish the endgame fix as the current private XR release

The maintainer asked to retain the existing product name and finish the
repository release. Publish `v1.2.17-xr-preview` from merged `main` at
`af439c3` as the non-prerelease “Latest” in the still-private repository.
Upload only the local source-built, release-signed, non-debuggable 10217 APK
and its `.sha256` file. APK SHA-256 is
`8913648e9c8c124367ac812a4a3e9db0d0f296ec3a2c7e83a7d22627c2182869`;
GitHub's asset digest and a fresh download agree. `adb install -r` succeeded
over 10216 on the connected Quest 3 without uninstalling; first-install time
was unchanged and the installed APK's SHA-256 matches the release. Android v3
signature, package/version, ARM64 ABI, absence of retail archives and
`RTS_DEBUG_CHEATS=OFF` pass. Endgame 33, panel text 20,469, console bridge
1,467 and workspace 781 host checks pass. The accepted 10216 short headset
test is the functional validation; do not claim a separate worn-headset test
of the exact 10217 bytes. Hosted Android CI remains unavailable because of
account billing/spending limits. Release-page guide/license links were made
absolute so they also resolve outside the repository file view. The retained
name and EA's trademark clause need a separate review before public
distribution; this release does not change repository visibility or include
the 10216 test/experimental LAN APKs.

## 2026-09-16: prepare the private 1.2.17 XR preview

The maintainer accepted the 10216 endgame test APK in the Quest headset and
requested an official repository release. PR #11 merged the tested source
fix. Reserve versionCode 10217 / `1.2.17-xr-preview` for a normal, privately
signed release build with `RTS_DEBUG_CHEATS=OFF`; never publish the 10216
test APK, whose retail end-action controller chords were enabled solely for
short verification. The repository stays private. A native rebuild, release
signing, update install, artifact digest and GitHub release are still pending
at this checkpoint because the hosted Android runner is blocked by account
billing/spending limits.

## 2026-09-16: review and accept the XR endgame test build

Review of PR #11 found that its post-frame-only polling could miss the
one-frame quick-end timer, and that the press coinciding with a new result
could dismiss the card before it was ever drawn. Review changes add pre-frame
polling, an end-action fallback for quick Skirmish victory, and first-frame
dismissal protection. The host endgame test now
passes 33 checks and bilingual panel text passes 20,469 checks. An ARM64
Quest APK with test chords enabled was built locally, versionCode 10216,
SHA-256 `051b7163d2199803d4c20eec0551350e46ba6a0bc4506d1603662e927f2379df`.
It has the established release certificate but is **not** a public release.
GitHub Actions cannot currently start because the account's billing/spending
limit blocks runners. The test APK was then installed as an in-place update
on the connected Quest 3 (`2G0YC5ZG9609PY`); device package inspection reports
versionCode 10216 and versionName `1.2.16-xr-endgame-test`. The maintainer
then reported that the headset test "works perfectly". This accepts the
observed result presentation, not every possible mission/network end path.
The 1.2.15 release APK was preserved unchanged.

## 2026-09-16: implement the XR match-result card (PR #11 initial submission)

Work from `main` at `0d8edb5` on branch `muse/xr-match-result`. Diagnose
the reported Skirmish win with no victory hint: retail
`MultiplayerScripts.scb` fires the normal `VICTORY` action
(`ScriptActions::doVictory`), which creates a non-modal
`Menus/Victorious.wnd` plus a 120-logic-frame (~4 s) end timer, then
`exitGame` leads directly to the score screen. XR tabletop shows a non-modal
window only in the small blended overlay above the detached build window, so
the message is missed; the Skirmish score screen itself carries no win/loss
marker (the per-player winner logic is commented out in `ScoreScreen.cpp`).
The quick end path (`doQuickVictory`, one-frame timer, no window) skips even
that. Confirm the end timer is wall-clock normal in XR (logic is
time-accumulated, independent of the render rate).

Implement a read-only match-result latch (`XrEndgame.h` state machine plus a
`XrGameBoot` adapter): Skirmish/LAN/replay results come from the
`VictoryConditions` trio (observers get a neutral match-over card, a locally
eliminated player keeps defeat), campaign results from
`ScriptEngine::isGameEnding` plus `CampaignManager::isVictorious`; queries
run only for a loaded interactive match with null guards. A head-yaw
billboard card (Canvas kind 2 with victory-gold/defeat-red/neutral accents,
German/English) appears with the latch, survives the direct transition to
statistics, and dismisses on any controller press or a new match; it never
blocks input. Victory conditions, simulation, network protocol and retail
data are untouched. Debug-only controller chords (both grips plus A/stick
clicks) fire the four retail end actions for short controlled scenarios and
are compiled out of release builds (standard `RTS_DEBUG` /
`_ALLOW_DEBUG_CHEATS_IN_RELEASE` gate; existing `debugVictory` untouched).

Host verification: new `xr-endgame-test.sh` (32 latch/dismiss/reset/pose
checks); `xr-panel-text-test.sh` extended with the bilingual card payloads
(20469 checks, +14 over the 20455 baseline, all passing); console-bridge
1467, workspace 781, scene 1280, hover-info 21, build-controls 4292,
loading-presenter 147 and first-run language 51 all pass; the edited
`XrPanelPainter.java` compiles standalone against android-34;
`git diff --check` is clean. The branding test needs a built APK and the
Android TU compile needs CI (`quest-build` label), since no configured
Android build exists on this machine.

Still required before merge: CI `quest-build` green, then a short
worn-headset check on a cheat-enabled build (four chord scenarios, dismiss,
score persistence, new-match clear, no card when quitting mid-mission). No
full-length match and no APK install while the user may be playing. General
controller keyboard and LAN stay out of this PR.

## 2026-09-16: hand off the missing XR match-result presentation

After the privately signed 1.2.15 release, the maintainer reports a complete
offline Skirmish win by eliminating every opponent, followed directly by the
statistics screen with no visible victory notice. The engine has both normal
victory/defeat windows and a quick end action; the observed path is not yet
proven. Make this the next bounded XR PR: diagnose the path, reliably show win
and loss in XR before usable statistics, leave simulation/score rules intact,
and test controlled end states without requesting another full-length match.
The general controller keyboard remains a separate task; the LAN-only IP/name
prototype is not part of the offline release. The maintainer also reports a
backup of the signing key/password and visually approves the screenshots;
the repo remains private pending an explicit visibility decision and the
end-result fix.

## 2026-09-16: publish the signed offline preview privately

Merge PR #8 as `683997a` and publish `v1.2.15-xr-preview` in the private
repository. Its 124,995,766-byte APK asset reports and independently downloads
with SHA-256
`bafff443d77e7b9e925a73fcf16b5bf7234c54f256e2cb7fa0f95d1aad008d2b`,
matching the Quest-installed candidate; the downloaded checksum file verifies.
The tag points to the merge commit. Mark all four older debug-signed releases
as historical prereleases without deleting assets. The new signed release is
“Latest”; repository visibility remains private. Public exposure still needs
signing-key/password backup and passthrough-image review.

## 2026-09-16: final signed Quest candidate accepted for core offline play

The maintainer confirms that the installed source-built 10215 APK with SHA-256
`bafff443d77e7b9e925a73fcf16b5bf7234c54f256e2cb7fa0f95d1aad008d2b`
starts both Skirmish and Campaign and retains the previous settings and game
data. The headset was not reinstalled during this validation. First-time data
import on a clean profile, every mission and a new full commands matrix remain
outside this exact-artifact check. Proceed with a private source-matched release
checkpoint; do not change repository visibility or publish a debug-signed APK.

## 2026-09-16: separate XR publication signing from debug builds

Start from merged `main` at `b515e75`, without the paused LAN branch. Keep
the P23 offline gameplay content, but produce a new 10215 non-debuggable XR
package with a private signing certificate and Android v3 proof-of-rotation.
The tracked debug key remains for development and the old end of the
lineage, with installed-data/permission continuity but no rollback right.
Prevent the hosted debug-build workflow from publishing GitHub Releases.
Local `assembleXrRelease` including vital lint, `assembleXrDebug`, signature
verification and no-retail-asset inspection pass. A clean ARM64/DXVK native
source build also passes; packaged `libmain.so` matches its newly built
counterpart at SHA-256
`28ac8c653c24b361c2eadf4f802dde2a6d565abf76f9b6e7bc106773380df382`.
Final source-built release APK SHA-256 is
`bafff443d77e7b9e925a73fcf16b5bf7234c54f256e2cb7fa0f95d1aad008d2b`.
After privately backing up the Quest app's small internal configuration,
`adb install -r` succeeds over diagnostic 10214 on Quest 3/API 34 and keeps
the same first-install timestamp. The final source-built APK is installed and
its device-side SHA-256 matches the local file. The first launch attempt is
intercepted by Quest's inactive-controller prompt. The user later played the
earlier signed repack, but that session was interrupted by our install of the
final source-built candidate; this is not evidence of a game crash. Exact-final
headset play, key backup and public-release review remain open. No GitHub
release or visibility change happens here.

## 2026-09-16: pause LAN and prepare the first XR release presentation

Preserve the Quest/Omarchy LAN trace and concrete next diagnostic on the
separate `codex/quest-pc-lan-preflight` branch; both peers agree at CRC frame 0
and first differ in the object checkpoint by frame 100. LAN is still not a
supported release feature. Prepare the `main`-based offline P23 presentation:
replace the inherited multi-platform README with a Quest-first overview,
include four maintainer screenshots after lossless metadata stripping, correct
the installation link from GitHub's older “Latest” to the exact P23 prerelease,
and add release/multiplayer handoff audits. The existing P23 APK download
matches its GitHub SHA-256, has the expected package/ABI and verifies with v2
signing. It is also debuggable and signed with a committed debug key, so public
signing and update migration remain explicit release gates. No new APK or
public GitHub release is created in this documentation pass.

## 2026-09-16: bound hosted CI to the Quest product

Stop running the inherited two-game Linux and macOS matrices automatically on
every XR pull-request or main update. This private repository ships the Quest
target, so desktop builds remain available only through `workflow_dispatch` as
explicit compatibility/release checks. Gate the full hosted Android build on a
`quest-build` pull-request label or manual dispatch and add per-PR concurrency
that cancels obsolete work. Local checks/builds remain the primary development
loop. Commit and merge this policy with `[skip ci]` to avoid consuming more of
the already exhausted monthly Actions allowance.

## 2026-09-16: implement P20.1 thin tabletop underbody

Halve the visible lower tabletop trim without moving the terrain datum. Add one
shared `XrBoardGeometry.h` contract: soil bottom remains -0.018 board widths,
the underside moves from -0.036 to -0.027 and trim thickness becomes 0.009.
Mesh construction, real-surface clearance, editing outline and diagnostics now
consume the same constants. Free-standing board poses, terrain registration,
world height and map coverage stay unchanged. Host checks pass: board 74,
height 272822, scene 1280, build controls 4292, menu 2869, menu routing 327,
interaction 135 and workspace 732. Work continues on
`codex/p20-1-thin-underbody`. Version the candidate as
`1.2.9-p20.1-thin-underbody` (10209), above release 10208 for safe update
installation. Android CI run 35055859028 passes and emits the 43,462,061-byte
ARM64 APK with SHA-256
`ef5c544cee9fd9212b71807a62ddfbfc1a33ce7feb22f28ff5dd8a7487ad9742`;
package/launcher metadata and v2 signing verify locally. Physical Quest edge
review passes on 2026-09-16 after update-installing and launching 10209 with
existing app data preserved. The user reports that the result looks good and
accepts P20.1 for merge; retain real-surface/underside checks in future tabletop
regressions. The user moves P22 keyboard/mouse farther back behind current
tabletop visual, content, campaign and controller quality work; controllers
remain primary.

## 2026-09-16: merge P20/P21 and publish XR Preview 1.2.8

Merge PR #2 (`c01ad1d`) after Android CI `35021079076` passes, then merge the
accepted P20/P21 work in PR #1 (`fc37591`) after Android CI `35027123032`
passes. The repository-wide generic Linux/macOS workflow remains a known red
baseline; it is separate from the Quest Android gate. The first PR artifact was
uploaded successfully but deleted by cleanup because a newer run from another
workflow shared the same branch. Commit `29fbcb9` now always retains the current
Android run's artifacts. Manual release CI `35032355387` passes and publishes
private preview release `v1.2.8-xr-preview`, version 1.2.8-xr-preview (10208), from
merged `main`.

The stable `Generals-Zero-Hour-XR.apk` release asset is ARM64, has package
`com.generalsx.zerohour.xr`, the correct product launcher and a valid APK v2
signature. SHA-256:
`0763b01dd0dfccfc99666a1a4dd614fdc0dde41a2f72282db9258eeee0927660`.
The release includes a matching checksum file and complete English installation,
retail-data and validation notes. Per user request, do not install final 10208
tonight; install it as a data-preserving update in the next Quest session. The
content-equivalent 10207 candidate remains the physical acceptance evidence.
P20.1's thinner tabletop underbody is next; P22 keyboard/mouse remains secondary
and QTR-MP remains the dedicated future multiplayer/replay milestone.

## 2026-09-15: P20/P21 headset acceptance and publication candidate

The user accepts the final P21 UI/Commands presentation, compact process-start
workspace, stable in-session Skirmish/campaign arrangement, explicit X/UI
whole-workspace alignment and supporting-stick home-base shortcut in the Quest
headset. Preserve the original radar distinction: Grip changes the map view,
Trigger issues an order; the alternate-mouse option swaps them. Promote the
default Android version to 1.2.8-xr-preview (10208), document the accepted
controls and move P20.1's thinner tabletop underbody to the next implementation
slot. PR #2's reproducible DXVK/Android CI foundation must merge first; PR #1
then consumes it and publishes the accepted XR source. A release APK and final
hash will be recorded after both PRs are merged and the APK is rebuilt from
`main`.

## 2026-09-15: supporting-stick home-base shortcut

Add a press edge on the supporting thumbstick (left by default, right for
left-handed controls). Dispatch the original local VIEW_COMMAND_CENTER message,
which moves the view to a command center or the most expensive owned structure.
No selection, unit-order or physical-table changes. Menu/focus/aim tracking,
camera locks, editing and construction preview gate the shortcut; its frame
consumes stick deflection before pan/zoom. Held buttons do not repeat or activate
on re-arming. Preserve native radar behavior and document Grip-to-look versus
Trigger-to-order (standard mouse scheme) in both controller-guide languages.
Host checks pass: interaction 135, handedness 264, console bridge 1433, panel
text 19593, workspace 732 and build-controls 4292. The native bridge also blocks
non-rotatable/line construction via pending placement type. Both Android APK
flavors, XR signature, native dependency closure and branding checks pass.
Installed 1.2.7-base-navigation (10207), data preserved and version verified.
Source 5b089cb/6f146a5, local integration be2fbaf; APK SHA-256:
`cb05a9fc3d49c089484cfc8a06ffa2e8c00198d935bfe2bfce0b547ddfe47844`.
Worn-headset verification pending. PRs stay unmerged until acceptance and CI
gates pass.

## 2026-09-15: P20.3 explicit alignment instead of deferred placement

User confirms manual table depth survives Skirmish-to-campaign and resets on a
fresh app process, as designed. A sideways glance still relocated the whole
workspace. Remove P20.2's deferred match-entry placement; the production initial
placement helper now latches exactly once per process, independent of game mode,
movie completion or camera readiness. Add **Align everything in front of me**
to the default UI/Windows page, with shared button/gameplay-X surface-detachment
confirmation. Cancel retains the actual poses; confirm moves the group rigidly
using horizontal heading only. Both languages and controller help are updated.
Host checks: workspace 732, interaction 120, menu routing 327, menu geometry 2869,
scene 1280, movie presenter 147, bilingual panel payloads 19593, build-controls
4292 pass. Native build and both Android flavors pass; signature, native dependency
closure, staged-versus-packaged libmain and branding checks pass. Installed
1.2.6-p20-alignment (10206) via data-preserving update, verified package version.
Source 5de20bd, local integration 922b6cd; SHA-256:
`84f383e69212ad7e16339932cd256cf1e3158db63126e567da30218eb87ab5d8`.
Worn-headset acceptance pending; PRs remain unmerged.

## 2026-09-15: P20.2 workspace transition and tracking follow-up

The initial P20 arrangement is accepted, but the user reports a sinking/moving
board separating from its build window and a low, off-axis campaign workspace.
No incident logs remain. Code inspection finds three actionable paths: initial
placement uses only launch heading; plain X recenters only the active surface;
nested campaign videos consume reference events without applying the pose
correction until returning to the outer loop. Also, valid-but-untracked poses
could drive placement/grabs during degraded tracking.

Align once at playable match entry unless the session placement was explicitly
chosen. Recenter all surfaces as a rigid group on gameplay X, retaining individual
reset in edit mode. Gate head/controller poses on valid AND tracked bits; cancel
input/grabs on loss and retain layout on recovery. Apply reference corrections
in both movie and game presenters through one path. Update in-game X help.
Host checks pass: workspace 684, interaction 115, scene 1280, loading presenter
147, menu routing 317, panel text 18625 and build-controls 4292. Native build and
both Android APK flavors pass; the signed 1.2.5-p20-tracking (10205) APK is
installed with data preserved and the installed version verified. SHA-256:
`cd4e9cd87674a6ea17a251ac3e026144a50915b76ae0730516493f7e7c264d9f`.
Source follow-up c253a7c, local integration 0036176; APK branding/signature,
native dependency closure and staged libmain match pass. Worn-headset stability
acceptance remains pending; PR #1 stays unmerged. Independently, PR #2 CI run
35007254486 exhausted runner disk during native compilation; no green CI claim.

## 2026-09-15: implement P20 safe unanchored session startup

Headset review accepts the P21 window artwork but exposes the previously queued
P20 issue: a new Skirmish inherits an oversized board and off-axis companion
windows from the prior room. The layout file contains relative geometry even
though the port owns no persistent room anchor. Keep its language, graphics,
handedness and map-coverage preferences, but reset all three spatial surfaces at
XR process start. The first valid HMD pose now receives the complete compact
photo arrangement: horizontal 1.65 m board, angled build window behind it and
the derived gravity-upright Commands window on the left. Explicit room/manual
placement and free adjustment remain session-local. Focused host tests pass. A
signed 1.2.4-p20-test integration APK (SHA-256
`85b95f302eaf16de4489ab267c6aad0fc31d0fae093386a22731b269adbe06a1`) is
installed with data preserved; Horizon blocked the first automated cold launch
because the controllers were asleep, so visible worn-headset acceptance remains
open.

## 2026-09-15: implement the P21 UI and Commands presentation pass

Implement PLAN-024 on `claude/p21-ui-command-windows` from `f27d450`. New
`GeneralsMD/Code/Main/XrPanelLayout.h` is the single layout source for both
spatial windows: one native control table drives Canvas pixels (new Java
`paint2`, no geometry on the Java side), UV hit testing and state rendering.
The Commands console gets a context card, grouped orders with chevron/armed
markers, instant/selection actions, a group number strip with count badges,
pending group operations and disabled-reason states; the workspace window
keeps its four tabs and wizard but groups controls into intent sections with
value chips, persistent edit-target selection and toggle states. Command
semantics, action IDs, handedness, saved layout and performance defaults are
unchanged; no P20, P20.1 or P22 changes are included.

Host verification: panel-text 18625, menu 2831, menu-routing 317,
console-bridge 1404, scene 1280, workspace 565, build-controls 4292,
input 27, tactics 2030, board 64, height 272818, math 111, camera 43,
comfort 260, interaction 109 — all PASS; `git diff --check` clean.
`XR_CURRENT_STATUS.md` and PLAN-024 record the new state. Open: CI APK
builds with hash, Quest install with preserved data, and the worn-headset
acceptance matrix.

## 2026-09-14: add the XR handoff dashboard and focused P21 contract

Add `XR_CURRENT_STATUS.md` as the short maintained entry point for another
coding harness. It separates implementation, automated/device verification and
worn-headset acceptance, records the current defaults, key source/test map,
Git/worktree handoff rules and the P20/P20.1/P21/P22/QTR-MP queue. Add a focused
PLAN-024 contract for redesigning the functional but visually unfinished `UI`
and `Commands` windows without changing simulation or network/replay behavior.
AGENTS and README link the new entry point.

The user also prioritizes a thinner tabletop underbody. Record P20.1 before P21:
halve the current lower trim from 0.018 to at most 0.009 board widths while
keeping the terrain transition, board top, picking, saved poses and companion
windows fixed. Mesh, surface clearance, editing outline and numerical tests must
share the new underside. This session creates the handoff specification only;
neither P20.1 nor P21 is implemented or physically accepted yet.

## 2026-09-14: prioritize the UI and Commands presentation pass

Add a dedicated P21 pass for the spatial `UI` and `Commands` windows before the
secondary keyboard/mouse investigation, which moves to P22. The new work covers
visual hierarchy, grouping, typography, spacing, panel sizing, contextual help,
command and manipulation states, localization, handedness and comfortable XR
placement. It must remove clipped content and large empty areas while retaining
all existing unit-command behaviour and multiplayer-facing semantics. This is a
planning update only; implementation and worn-headset acceptance remain open.

## 2026-09-14: keep returning launches inside XR

User reports a visible importer flash and a switch from passthrough into the
Meta home environment before gameplay. The previous launch gate finished the
immersive Activity and always routed through a 2D setup Activity, even with
valid configured files. Move saved-data validation to a worker owned by the XR
entry itself. Continue into the game in that same Activity on resume; open the
importer only for missing/unusable data. Remove the UI round-trip launch ticket.
Reuse path recovery with setup, and avoid rewriting already matching native
markers/preferences. Interrupted/destroyed startup cannot dispatch a stale
launch; pause/resume cannot start the engine twice. No renderer/native changes.

Regression checks cover silent validation, identical resolved paths, absence
of importer launches during the check and unchanged marker timestamps. Both
flavors are built; normal Quest launch must show a direct XR start without
GameDataSetupActivity in the Activity launch trace. Worn-headset smoothness is
still a user check. No GitHub publication is authorized in this slice.

## 2026-09-14: guided Steam/installed-retail data setup

Replace first-run advanced settings with a shared prepare/select/review flow.
Preserve system-language selection, expose English/Deutsch directly, and keep
game text/speech independent. Explain installed Steam and CD/ISO folders,
base-game dependencies and the lack of a Windows installer/raw ISO extractor.
Bounded folder discovery resolves nested/adjacent/merged data and stores the
actual native base root. Validate BIGF tables against file length, allow the
real PatchZH empty Data\\* placeholder, and require expansion content/text.
New selections stay drafts until revalidation and confirmation; atomic native
markers follow the paired preferences. Both pickers resume after permission.

XR checks before its render thread, and never-started redirects no longer
call the native stop hook. Android enters setup before SDL initialization.
A device test found Horizon recreating stopped panels with an older state
Bundle; preserve the latest draft in the Activity Intent too. No native engine,
renderer, game rules or multiplayer eligibility changes in this slice.

Validation: host fixtures and the real local Steam dataset pass; both Android
flavors and the separate instrumentation APK build. The Quest fixture passes
16 checks against its actual data, translations, unconfigured/invalid selection,
recreation and cancellation, with unchanged preferences/native markers.
The final build/launch evidence is recorded in `build/quest-data-setup/`.
Normal launch also reaches OpenXR FOCUSED stereo frames through the new gate;
the separate test package is removed after validation.
Individual raw CD/ISO editions and a fresh-install permission round trip still
need physical acceptance; do not describe the fixture as an ISO installer test.

## 2026-09-14: Generals: Zero Hour XR product identity

User accepts P19.1 and requests consistent public branding before GitHub
preservation. The chosen name is Generals: Zero Hour XR. User explicitly
chooses to keep com.generalsx.zerohour.xr, avoiding a separate app/data migration.
Use the product name for the launcher/app label, setup titles, localized
diagnostic subjects, OpenXR application/action-set identity, workspace heading,
README and product-named APK export. Override XR resource variants for every
existing locale, so localized main resources cannot resurrect the old name.
The ordinary Android flavor, Java/JNI namespace, save paths, original assets,
engine attribution and license notices remain unchanged.

Native and both APK builds pass, as do 235 panel and 1,280 scene assertions.
Branding checks verify 89 compiled labels and all 13 source locale overlays.
Both APK signatures verify. Quest update succeeds with identical 342-byte
layout and matching APK hash; cold launch returns OK. Launcher readability
and gameplay remain physical checks. Evidence: build/quest-branding/validation.md.

The source snapshot is published privately at
`Cesarus85/Generals-Zero-Hour-XR` (`main`, commit
`0e7cc9e1cd986779b161c8839feb845a3b028ba0`). Release
`xr-preview-2026-09-14` uploads the 215 MB product APK and SHA-256 sidecar;
GitHub's asset digest matches the local APK. The repository remains private.

## 2026-09-14: P19.1 optional play-space wizard and reference stability

User reports basic P19 surface placement works, but their table is missing,
placement can jump in height, and setup ordering is confusing. Implement
the approved stabilization slice without changing simulation or graphics
quality. LOCAL reference-space changes now compensate all workspace poses
using the inverse new-origin pose, never the current head height. Invalid
origin transitions hide untrustworthy placement and request explicit recovery.

Setup works before a match as well as during eligible tabletop gameplay.
Default remains a freely arranged board with no scan or real-furniture binding.
Dedicated DE/EN wizard pages offer free placement, detected surfaces or manual
controller-height placement, then preview and confirmation. Capture completion
reloads room data on focus return; system capture/permission does not time out
through the startup-only idle timeout. Plane counts and cyan boundaries separate
detection from fit; invalid board footprints stay visible in red. Prefer actual
2D tabletop bounds over a 3D volume. Fit starts enabled and remains optional.
Preview-only stick size/yaw changes and manual height need fresh trigger presses;
cancellation preserves the live layout. No persistent anchors across restarts.

Validation and hardware acceptance are recorded in PLAN-023 section 46.
P19 acceptance, not compilation alone, still gates GitHub source preservation.
Native and both APKs pass; P19.1 installs with matching hash and unchanged
370-byte layout. Actual Canvas verifies 88 panels; GPU verifies 63,299
cumulative compositor assertions. Posture/room-placement acceptance is open.

## 2026-09-14: P19 room surfaces and native hover prices

User confirms P18.1 works. P19 adds opt-in USE_SCENE permission, native room
query/capture, table/floor selection, bounded preview and explicit placement.
Plinth underside rather than terrain center is aligned. Companion windows keep
their pitch; fit-to-surface is optional and only shrinks. Focus/tracking loss,
native dialogs and recenter cancel preview; normal input must re-arm. No
persistent room anchors, continuous snapping, engine rules or multiplayer
eligibility changes.

Original ControlBar tooltip evaluation now supplies XR prices, requirements,
purchase restrictions and upgrade/science status. Read-only native tooltip
picking includes disabled/static widgets and the expanded abilities tree.
Money/power/experience hints are surfaced. Larger hover cards paginate long
descriptions without losing lines. Existing DE/EN controller guide explains
the surface workflow and hover behavior.

Host tests, native build and both Android APK flavors pass. Quest Canvas/GLES
fixtures pass; room permission/placement and real native purchase-hover comfort
still require the user's worn-headset acceptance. See PLAN-023 section 45 and
build/quest-p19/validation.md for scope/evidence. After that acceptance, create
a clean source GitHub repository with a roadmap; **not before**. QTR-MP remains
a separate retained milestone.

The first installed P19 package exposed a manifest integration defect: Quest
reported scene=0/capture=0 and Load room data could not query. Adding the native
USE_ANCHOR_API capability alongside the separate USE_SCENE permission fixed
extension availability. The corrected package boots with scene=1/capture=1.
No user permission was force-granted; physical acceptance remains pending.

## 2026-09-14: P18.1 controller build rotation and discoverable workspace controls

User approves building orientation, selected-surface feedback and an in-game
controller reference. Supporting Grip + pointing stick now rotates the actual
native placement ghost, with original validation/order semantics retained.
Dominant Grip still cancels; additive selection remains outside construction.
Capture sticks until neutral and freeze orientation through placement-trigger
release. Reject UI/modal/tracking, non-XR, missing ghost and line-build cases.

Selected window target receives persistent orange + check mark and a matching
physical outline, including cropped/expanded build windows and the P18 plinth.
It is editing-only overlay geometry, not another world render. UI -> ? opens
four DE/EN controller pages with actual left/right and A/B/X/Y substitutions.
Existing native command-console help is retained. No pose/quality migration.

Host regressions and the extracted native rotation adapter pass. Actual Quest
Canvas renders 68 panels, all 16 controller guide variants at 26 px without
clipping; screenshots inspected. Real GLES compositor verifies orange rim /
transparent center and retains existing stereo/world-omission checks. Native
and both APK flavors are built. These tests do not establish worn-headset
construction/comfort acceptance. PLAN-023 section 44 records scope and gates;
build/quest-p18-1/validation.md records final deployment evidence. Proposed
real-surface placement remains P19, not silently bundled into this change.

Installed P18.1 with matching base.apk hash and both APK v2 signatures verified.
The actual 361-byte v10 layout is unchanged. Startup waits at the normal
controller-required dialog, no game PID/bypass. Isolated Canvas QA removed;
retained P18 rollback and P18.1 evidence do not replace in-headset acceptance.

## 2026-09-14: P18 fixed tabletop base and terrain-independent mapping

User calls P17 the major unlock and authorizes the three height/picking
follow-ups. P17 is now positively accepted for perceived playability; the
previous read-only runtime sample confirms roughly 590-1,230 omitted draws
per native frame, High/Multiview and no recovery in those sampled intervals.
That is not a controlled FPS measurement. Retain P17 as rollback.

P16.1 diagnosed but did not fix height movement. P18 replaces center-terrain
Z with the native loader's map-wide minimum from TerrainLogic::getExtent.
It survives panning, boundary changes and cinematics, and follows each newly
loaded map without pointer/first-view caches or a new terrain scan. Center XY
comes from the native look-at point, yaw from its angle. Footprint scale now
uses desired camera height/FOV and the W3D default-pitch distance convention,
not three terrain-ray intersections or native terrain-following corrections.
User zoom/coverage/table size still work; no native camera or simulation edit.

The soil edge still samples real terrain, but the plinth top/underside stay
at -0.018/-0.036 board widths. No edge-minimum prepass. Rendering, inverse
picking and feedback use one matrix. A shared CPU/GLSL ceiling covers the
native heightmap byte range above the datum plus the former half-board of
object headroom, preventing close-zoom hilltops/markers from being chopped.
Uses the already-uploaded board matrix, no additional per-draw uniforms.

Host regressions pass (8,775 previous checks, 23 extracted mapping-adapter
checks and 272,818 parametrized height/plinth/render-ray assertions). Actual
Quest GLES passes 1,339 allocation/shader checks with high-terrain cases,
77 world/feedback checks, 63,293 cumulative state/compositor and 3,321
copy/publication checks; the height suite also passes on ARM64. These are
synthetic fixtures, not full native collision/build-placement acceptance.
Layout v10, window poses, quality and P17 omission remain unchanged.
PLAN-023 section 43 records scope, artifacts and the worn-headset gate.

P18 installed with matching base.apk SHA-256 and both APK v2 signatures
verified. The actual 363-byte v10 layout remains byte-identical. Normal
launch waits at Horizon's Controller Required dialog, no game PID/bypass.
Activate both controllers for real-game scrolling/selection/build acceptance.
Build artifacts, source delta and logs are preserved in build/quest-p18/.

## 2026-09-14: P17 conditional ordinary-world omission, device-test candidate

The user explicitly approves eliminating the redundant flat-screen world.
Auto now conditionally suppresses the ordinary submission, in addition to
the existing planar snapshot omission. Require a current eligible stereo
draw, certified coverage for the current allocation, allocated UI MRT and
healthy Multiview. Reference/Atlas, first allocation, explicit PPM capture,
UI and RTT retain ordinary rendering. No resolution/detail reduction or
second traversal; dynamic buffers are still consumed immediately.

Track incomplete ordinary contents through actual native Presents, not
outer simulation frames. Empty/partial/depth-only clears cannot resurrect
missing pixels. A full backbuffer color clear establishes a fresh base.
The normal world and movie Begin_Render paths already clear. Same-draw
Multiview shader failure still renders both eyes with Reference shaders.
If neither valid stereo nor a complete ordinary image survives, suppress
the partial image and input, show a localized recovery card, and require
full ordinary rendering for subsequent frames. Nested movie presentation
observes and clears this recovery state too. This is not exact same-frame
ordinary reconstruction: that would require unsafe mutable-buffer replay
or a separate replay architecture. PLAN-023 section 42 records this limit.

Settings expose Extra world: Auto/Always,
retaining the prior session setting/default. Presentation reports actual
omission, and a bounded 120-Present counter reports saved submissions.
This is a headset-test candidate, not an accepted campaign FPS improvement.
8,775 host checks pass. Quest GLES tests pass 46,905 stereo checks / 63,293
cumulative compositor checks, 3,321 copy/publication checks, 427 allocation,
8,667 uniform-cache and 73 world-shader checks. Fixtures use extracted
production paths; they do not replace worn-headset campaign/menu testing.
Layout v10, the P16.1 setback, tabletop height and simulation are unchanged.

Both APK v2 signatures and installed Quest SHA-256 verify. Installation
preserves the actual 360-byte v9 layout. Normal launch still waits at
Horizon's Controller Required dialog; no bypass. The pending P16.1 v10
migration and real P17 gameplay/saved-draw/FPS checks remain open. Artifacts
and bounded source delta are saved in build/quest-p17/validation.md.

## 2026-09-14: P16.1 build-panel setback and tabletop-height investigation

User reports P16 plays well, asks for the next performance candidate, reports
intermittent tabletop height changes, and requests the build panel slightly
farther away. Move that panel 10 cm away along launch heading; leave height,
orientation, scale and other surfaces alone. Layout v10 performs the offset
once for v7-v9, while older photo migrations receive the new preset directly.
Pre-v9 quality migration is retained; deliberate v9 quality is not reset.
Guard the existing 5 m layout bound. New starts and Photo Arrangement agree.

8,537 host checks pass, including real v9 parsing, one-shot migration,
save/reload, later custom placement, tilt/input geometry and existing game
bridges. A separate probe migrates the actual 360-byte device layout:
UI depth -1.080 -> -1.180 m, all other fields unchanged. Native and both APK
packages build successfully. Rendering remains P16; no optimization or height
behavior change is bundled into this placement update.

Read-only P16 logs confirm actual Multiview/Balanced and 96-98% uniform-cache
hits in sampled gameplay. These percentages are not FPS; the latest scene
varies substantially and cannot establish a controlled P15/P16 speedup.
Source confirms the ordinary composed-world draw is still unconditional,
making safe elimination the next larger performance experiment.

Height: center-ground mapping changes Z with the camera; the visible base
also follows the minimum sampled edge height. A production-header probe
reproduces an 8.25 cm base shift for a 0.05 normalized edge-height change at
1.65 m board width, with the surface pose unchanged. Logs show center height
15.6 -> 49.6 -> 15.6 and a constant sampled in-game panel pose, with one
layout-anchor event. The user's uncertain report that the build panel stays
put is consistent, not definitive worn-headset attribution. Propose a stable
physical plinth and map/session height datum before the next renderer change.
PLAN-023 section 41 records the scope and remaining gates.

P16.1 installed; installed hash and both APK v2 signatures verified. The
360-byte v9 layout is unchanged. Normal start waits for active controllers,
so in-headset migration remains open. P16 APKs and a matching pre-v10 layout
are retained for rollback; the older app cannot read the newer format.

## 2026-09-14: P16 preferred multiview and per-program uniform caching

The user reports P15 Multiview looks equivalent and plays reasonably well,
and approves making it the preferred path plus reducing uniform churn.
Read-only game logs confirmed actual Multiview, but did not establish a
controlled campaign FPS advantage. This supersedes P15's default decision.

Multiview is now the session default, with capability/allocation/shader
fallback and manual Reference/Compact comparison retained. No new layout
migration: fresh Balanced, Light shadows and Auto world copy stay as before;
deliberately saved High and custom placement are not reset.

XR ProgramInfo now retains independent texture-matrix, miscellaneous,
material and lighting uniform keys across program switches. External GL
ownership changes advance a lazy invalidation epoch. Object matrices still
upload each draw, the shared view/projection UBO remains independently cached,
and non-XR uses the existing global policy. No scene traversal, ordinary world
draw suppression, buffer upload policy, shader math or quality reduction.

Both native/APK builds and 8,492 host checks pass. Quest GLES tests pass:
8,667 new uniform-cache checks, 45,077 cumulative stereo/compositor checks
(plus 12 benchmark checks), 427 Multiview, 3,285 copy/MRT and 73 world/shadow.
The dynamic uniform fixture drops uploads from 24,282 to 11,598 (52.2%).
An isolated static-material benchmark takes about 18.3-18.7 ms versus
21.4-22.8 ms for 1,800 tiny submissions. This is not in-game FPS or a
promise of campaign gains. PLAN-023 section 40 records scope and acceptance.

Installed XR APK hash matches; both v2 signatures verify. The current 362-byte
v9 layout is unchanged immediately after installation. Normal launch awaits
Horizon's Controller Required dialog, with no game PID and no bypass.
Active-controller campaign acceptance remains open; P15 rollback is preserved.

## 2026-09-14: P15 tabletop defaults and opt-in true multiview

Implement the user's requested combined step: Balanced / Light / world-copy
Auto, tabletop-only gameplay controls, and true multiview as a comparison path.
Layout v9 migrates quality/view intent once while preserving current custom
poses and independent preferences; later deliberate High persists. Both legacy
controller view toggles and manual normal-view/startup actions are removed.
Campaign videos, shell and technical fallback keep upright full presentation.

OVR_multiview2 uses array color/depth-stencil targets, a distinct two-view
generated shader and one immediate stereo draw per eligible source draw.
Compositing samples array layers directly; ordinary world rendering, UI MRT,
coverage, decorations and safe per-eye recovery remain available. Capability,
allocation and shader failure have explicit fallback. GPU timer queries are
disabled for requested multiview because OVR defines their results as undefined;
CPU/FPS measurements remain available. Reference remains default.

Native/both APK builds, signatures and 8,492 host checks pass. On Quest:
45,077 cumulative production draw/state/compositor checks, 427 new allocator/
shared-shader/coverage checks, 3,285 copy/MRT checks and 73 world/shadow checks
pass. Fixtures are not gameplay acceptance or evidence of improved campaign FPS.
Program-switch uniform overhead remains a profiling candidate. PLAN-023 section
39 contains architecture, reproduction and the physical A/B gate.

P15 installed, installed APK hash verified, 358-byte layout unchanged directly
after installation. Normal startup waits at the system controller-required
dialog; actual migration and campaign performance still need active-controller
headset acceptance. P14 rollback remains preserved.

## 2026-09-14: P14 conditional planar-world copy, unchanged eye quality

User reports P13 plays substantially better and authorizes the proposed next
optimization. Read-only P13 log retrieval shows no prior OpenAL buffer-deletion
errors in that sample; it also records the user's Compact/High experiments,
not a controlled renderer comparison. Preserve P13 as the working rollback.

At the native UI boundary, omit the 1280x720 planar-world blit only after the
current frame's stereo coverage/readiness gate passes. UI MRT and ordinary
world/composed rendering stay intact. An omitted planar snapshot returns no
texture, so late stereo loss uses the current composed frame instead of an old
world image. Unready/flat frames copy normally. Add session-only DE/EN View ->
World copy: Auto/Always, Auto default; independent of Compact/Reference and
included in measurement epochs. Counter logs report actual copies/skips.

Native/both APK builds and 8,437 host checks pass. Quest: 3,285 new actual
UI-boundary/copy/MRT/fallback checks, existing stereo/compositing checks and
73 world/shadow/allocation checks pass. Small boundary benchmark saves about
0.04–0.08 ms/frame, not a game FPS claim. The extra ordinary world draw remains
because it provides same-frame fallback; removing it needs a separate recovery
design rather than another drawViews traversal. PLAN-023 section 38 records the
implementation, evidence and remaining physical acceptance gate.
Installed P14 on Quest with matching base-APK SHA-256 and unchanged 358-byte
layout. Normal app startup waits for controllers; no P14 game FPS claim yet.

## 2026-09-14: P13 audio leases, truly inactive timing, optional stereo atlas

P12.2 did not pass user performance acceptance. Light/Balanced feels worse;
turning Timing off helps only slightly. The captured runtime contains repeated
OpenAL buffer-deletion exception stacks inside audio update (also present in
P12.1). Fresh cache entries incorrectly started with zero leases; closing them
could underflow. Loop/portion transitions released the lease while the stopped
source was still bound. Fix both ownership errors, without reducing sound or
graphics quality. A silent real-OpenAL test reproduces the error and verifies
300 safe replacement cycles. Host tests exercise the production cache code.

Timing OFF also still polled GPU disjoint status twice per frame. Now disabled
or empty timer rings issue no polling calls; pending queries remain owned and
are drained with epoch rejection when timing resumes. No blocking waits added.

Add a session-only Compact/Reference stereo toggle. Compact allocates both eyes
side by side, keeps all per-eye pixels, scissor-isolates rasterization and clamps
compositing samples at the seam. Production draw/state/pixel and real quad-shader
checks pass on Quest. Repeated small benchmarks show no reliable overall gain;
keep Reference as default and do not claim faster campaign FPS. PLAN-023 section
37 records evidence, fallback, tests and the remaining physical acceptance gate.
Native/both APK builds, 8,360 host checks and the Quest hardware suites pass.
P13 installed with the 373-byte layout unchanged and installed APK hash verified.
Horizon currently requires active controllers before normal app startup;
campaign performance acceptance remains open.

## 2026-09-14: P12.2 narrow stereo state restoration

Reduce confirmed redundant fixed-state work after each immediate stereo draw.
Restore only eye-modified state; keep the full-state cache valid and set the
shared eye viewport/depth range once per pair. No quality, shadow, geometry,
buffer ordering, simulation or gameplay changes. Fresh logs establish recurring
cache misses, not a controlled Light/Balanced CPU/GPU bottleneck measurement.

Native/both APK builds and 7627 host checks pass. On Quest, 10250 checks compare
actual production draw/state code against full restoration over 1024 scenarios:
same pixels/state/dispatch, 612 full applications avoided. Existing production
shader/eye-allocation hardware checks also pass (73). Installed P12.2 with layout
preserved. PLAN-023 section 36 records artifacts, boundaries and the still-open
same-scene campaign/Skirmish frame-time and gameplay acceptance gate.

## 2026-09-14: user chooses fluid gameplay over replacement shadows

User reports about 36 FPS with Light shadows + High, approaching 50 with
Light + Balanced. Record these as user observations, not compositor telemetry.
Adopt Light + Balanced as the working performance baseline; defer replacement
shadows explicitly and prioritize consistent frame times and input response.
PLAN-023 section 35 records the direction and sustained/combat validation gate.
Documentation only: no runtime default, persistence, APK or device changes.

## 2026-09-14: P12.1 campaign flat-mode recovery and cinematic camera guard

Fresh P12 logs confirm the user's campaign never enters split view, while
subsequent Skirmish does. Saved quality is Balanced; flat uses the 1280x720
canvas. Find two defects: Tabletop actions require an existing split capture
and do not release the legacy upright override; automatic camera favorites
call native user methods that cancel scripted movement even after its short
input lock expires. The recording shows the favorite before any split capture,
but did not log blocking flags, so the sole runtime cause is not proven yet.

Separate requested view from capture readiness, preserve explicit choices
through startup, and defer automatic camera presets until a completed split
capture and a non-cinematic state. Guard all XR camera methods against movies,
letterbox and scripted paths. Show actual flat/stereo extent and fallback reason
in View and bounded logs. No scripts, assets, layout migration or resolution
allocation changes; preserve the user's High/Balanced preference. Production
regressions, build/install evidence and the fresh-mission gate: PLAN-023 section 34.

## 2026-09-14: P12 campaign shadow A/B and asynchronous CPU/GPU attribution

User confirms that P11.1 campaigns now run, but reports worse performance and
appearance than Skirmish, then authorizes the proposed profiling step. Existing
recordings show a campaign renderer-counter median of 16.7 versus 56.7 in
Skirmish and about 452 versus 212 shadow draws/frame. These are different
scenes, not a controlled benchmark or compositor FPS measurement.

Expose session-only Original/Light shadows and opt-in measurements directly
under workspace View, in DE/EN. Original remains default; Light skips volume
shadows while retaining existing decals. This is an explicit quality tradeoff,
not new contact shadows. Preserve resources for instant reversal and the old
scoped restoration of non-XR graphics flags. No resolution, layout-v8, scripts,
game orders, simulation cadence or multiplayer eligibility changes.

Add optional context-local EXT_disjoint_timer_query dispatch with an eight-slot
asynchronous ring. Never wait/read an unavailable result, reject disjoint and
stale-profile samples, stop the query before nested loading presentation, and
warm up after mode/focus/settings changes. Logs distinguish engine CPU wall,
eye composition CPU wall, xrWaitFrame and GL engine elapsed time. GPU elapsed
is neither GPU occupancy nor total compositor timing. See PLAN-023 section 33
for controls, limits, artifacts and the required same-scene A/B/A acceptance.

Native and both signed APKs build; 7552 host checks pass (including 449 actual
frame shadow-scope checks). Quest ARM64 passes 161 timer-policy checks and 91
real Adreno query checks. P12 installed with matching SHA-256; actual 360-byte
layout unchanged, P11.1 Locale APK retained. Normal launch still waits for
Horizon's controller-required dialog. No live campaign improvement or final
visual acceptance is inferred from fixture timings.

## 2026-09-14: P11.1 first-run system language follow-up

User asks for German on German systems and English everywhere else, on first
start only. Previously XR hardcoded German while Setup defaulted game text to
English. Share a tested Java DE/EN policy; read Resources.getSystem rather than
the launcher-overridden Locale.getDefault. Pass the initial XR language through
JNI and adopt it only when no saved layout restored. Preserve existing v8 and
legacy choices. Setup and direct/legacy XR entry seed native game text only
when no prior choice exists. Verify German text assets before requesting them;
otherwise explicitly use English. Audio, movies and SKU artwork stay as supplied.

The Locale APK variant builds for zh/xr, passes 6893 host checks including 51
Java language cases, and is installed with a matching device hash. Existing
layout is byte-identical. User device reports de-DE; no OS language change,
data clearing or real first-install reset was performed. See PLAN-023 section 32.

## 2026-09-14: P11.1 arrangement exit, science window and campaign presentation

Fix the user's three reported regressions. The workspace entry button becomes
DE/EN Done during arrangement; Back/A exit before menu/console ray capture and
all close routes re-arm only after controls return to neutral. The native
science-tree overlay now requests the entire detached UI canvas, with the same
bottom-edge continuity and input ownership as quit/options/communicator.

The campaign recording identifies a presentation starvation bug, not a crash:
SinglePlayerLoadScreen keeps decoding/rendering and feeding audio inside a
blocking loop, while the outer OpenXR frame never returns. Add a scoped,
presentation-only callback after loading draws and repeated cinematic camera
draws. End the interrupted outer frame once, submit fresh predicted eye poses
with passthrough and an upright full game panel, then reacquire gameplay poses
after the original loader returns. Never recurse into engine/simulation update.
Held launch clicks cannot skip; release then Trigger/Back can skip the original
movie. Fullscreen InGameUI videos also suppress split/stereo presentation.

P11.1 Quest/Android builds and regression evidence are recorded in PLAN-023
section 31. Preserve layout v8, user game data, the P11 rollback APK and current
layout backup. Physical campaign/Challenge, science purchase and arrangement
acceptance remain distinct from compilation and isolated device fixtures.

## 2026-09-14: P11 direct tactics and offline-Skirmish boundary

Implement a direct collapsible Tactics area in the command console: original
formation toggle, force move, guard-object/escort, guard without pursuit, and
four native-camera map bookmarks. Keep old button size/top anchor, add DE/EN
help/hover explanations, invalid-selection reasons and focus/tracking cancel.
Use existing engine commands and ViewLocation, never custom simulation rules.
Mixed formations expose a native first-member limitation and are blocked with
guidance instead of changing the shared AI formation implementation.

User explicitly asks to retain multiplayer. PLAN-023 section 30 records QTR-MP:
lobby/join, chat/team text input, beacons, diplomacy, observer/replay and native
network determinism/device gates. Offline AI Skirmish remains supported now;
human LAN/internet and replay stereo remain gated, including human games with
AI slots. No network messages to other players or remote service changes.

Final build, test, deployment evidence and the still-open worn-headset checklist
are in PLAN-023 section 30. Layout v8, graphics budget and original assets stay.
Native/both APK builds, 6148 UBSan host checks, 5296 Quest ARM64 checks and 38
real Android Canvas panels pass. Installed P11, exact hash verified:
`caeed1aa288572c0b7f51ea46ddfae4906be8a18d28614b602466e018928d642`.
The saved layout was still v7 (332 bytes), backed up with the installed P10.3
APK before update. Normal startup waits at Horizon's controller-required dialog.
No worn-headset or human-network acceptance inferred; wake controllers to play.

## 2026-09-14: P10.3 upright console, groups guidance and bilingual XR

User's new photo exposes inherited pitch/roll in the inward-facing console.
Keep docking but rebuild its rotation from heading and world up. Native
Communicator is Diplomacy.wnd, not PopupCommunicator: expose actual visibility
for full-canvas rendering and input ownership, with a direct console shortcut.

Clarify native group semantics (add GROUP to SELECTION, not new members to a
group), show live counts, reject empty recalls/saves with guidance, and add
three in-place help pages. Center all ten slots with the native camera call;
the old shared view-team translator excludes slot zero. No group protocol edits.

Add an extensible DE/EN catalog, localized Canvas payloads and v8 persistence
without resetting v7 poses. XR changes immediately; original game text requires
an available language pack and a restart. Honor the existing text-only marker
in XR boot. The device currently has English data, not German; no game assets
are downloaded or replaced. Original audio/branding and launcher locale remain.

Native/both APK builds pass, as do 4931 UBSan host checks, 4133 Quest ARM64
checks and 26 real Android Canvas panels. app_process lacked initialized fonts;
an isolated Activity fixture passed and was removed afterward. Flat text/layout
tests do not establish in-game communicator, groups or headset comfort. Command
coverage audit and deployment evidence: PLAN-023 section 29.

Installed P10.3 with exact installed APK hash verification
`b0c073cdef29a0ae800b808e07f5fdee38626ddcb57fbf60a1ee067964667844`.
Horizon's controller-required dialog intercepts startup; no game PID present.
P10.2 APK and the actual 332-byte v7 layout are preserved for rollback.

## 2026-09-14: P10.2 photo arrangement, HQ eyes and complete game menus

User provides a headset photo and requests it as default, sharper graphics,
complete native game-menu windows, and explanation of guard commands. Read the
current v6 device layout as geometric evidence. Round/recenter its table/build
poses, angle the left console inward, and default eligible offline matches to
stereo. v7 applies the requested pose/startup change once and saves quality;
later custom poses remain. UI → View offers Photo arrangement and High/Balanced.

Raise stereo width budget 1536→1920 within the existing 2048 extent cap. Keep
the native game/UI canvas and original assets unchanged. Quit menu does not
necessarily use the modal stack: detect quit/modal/options state and expand the
UI canvas upward only while needed. Board stays behind split-compatible dialogs;
input/camera commands are blocked until returning. Add explicit target-click
hints for guard/movement; native orders and original menus stay authoritative.

Native and both signed APKs pass, with 4091 UBSan host checks, 3356 Quest ARM64
checks and 73 Adreno shader/eye-allocation checks. Preserve P10.1 APK and the
current 363-byte v6 layout before installing. No worn-headset dialog, arrangement
or sustained HQ performance acceptance is inferred. Installation succeeds and
the installed APK hash matches the new artifact. Horizon OS blocks startup at
its controller-required dialog; no game PID is present. Touch controllers must
be awakened for physical acceptance. Full evidence: PLAN-023 §28.

## 2026-09-14: P10.1 relaxed-arm picking, map navigation, handedness

User played P10 and reports it initially feels very good, superseding the
earlier controller-required startup observation. Distant picking requires an
uncomfortable raised arm, map pan is ineffective, and left-handed roles are
requested. Confirm two legacy screen-GUI refusal paths under the spatial ray,
terrain-only click eligibility, grazing-terrain drag amplification, console
early-return pan interception, and near-plane-unit scroll scaling.

P10.1 gates screen-UI refusal only for spatial input, permits object-only hits,
re-picks short clicks using the press ray, classifies drag on a bounded ray-facing
plane, and pans in board/world units through native user camera control. Bind
both physical Touch hands once; map them to dominant/support roles selected in
UI → View. v6 persists handedness and migrates old layouts; switching closes
settings and requires neutral controls. Native game rules remain authoritative.

Native and both signed APKs pass. 3683 UBSan host and 2735 Quest ARM64 fixture
checks pass; none substitute for actual controller comfort or order execution.
Preserved P10 APK and the current v5 device layout before update. Artifacts,
hashes, full hand mapping and acceptance checklist: PLAN-023 section 27.

Installed P10.1 with data preserved and verified the installed base.apk hash
6682741fb7f9fca7adb64ea7ee03fc72feb9919a525ed4ff4530d825312e3088.
This new launch is again intercepted by Horizon's controller-required dialog;
no game PID is running. Asked user to wake both controllers. Hardware fixture
success is not an OpenXR runtime binding or worn-headset acceptance claim.

## 2026-09-14: P10 installed, controller wake required

Quest 3 reconnected as 2G0YC5ZG9609PY. Backed up its actual current v3 layout
(320 bytes) to build/quest-p10/pre-p10-layout-20260914.cfg before installing
the prepared P10 APK with data preserved. Installed base.apk SHA-256 matches
2fdd81ff1943bf015f2fba5d62f6a866f3777302816013c1bcdef40bc481d23c.
Startup reaches Horizon's controller-required dialog, not a P10 game process.
User asked to wake controllers and test direct rectangle selection. No new
gameplay, visual or worn-headset acceptance is claimed.

## 2026-09-13: P10 direct drag and persistent command console

User rejects two-corner/menu-first interaction as the primary control path.
Implement deferred trigger intent: short click keeps native context, 2 cm
table-space drag selects a live rectangle on release, left grip adds/toggles.
Armed construction/abilities and targeted orders keep precedence; cancellation
never falls through to movement. A docked nonmodal command console exposes
16 actions and ten groups directly, with one-shot save/add/center modifiers.
A toggles it in live play, while layout arrangement stays in workspace UI.
v5 preserves console visibility and migrates old poses/preferences.

Native and both signed APKs pass; 3399 UBSan host checks pass, including the
production trigger function compiled verbatim with spies. Focused ARM64
fixtures compile but are not device-executed. Quest was disconnected, and
the user went to sleep and deferred device work until morning. No P10
installation, headset or visual acceptance is claimed. Ready APKs, prior P9
rollback APK and logs are under build/quest-p10. See PLAN-023 section 26 for
the exact install/backup and controller test sequence.

## 2026-09-13: P9 tabletop body and controller army commands

User accepts the P8 direction and requests all four next items, prioritizing
unit control. Add two-corner world selection, additive selection, ten native
hotkey groups, movement/attack-move/force-attack/guard/stop/scatter and queued
movement through the existing engine. Keep native build/ability UI semantics.
Respect selection caps and publish matching simulation selection messages;
detached UI no longer blocks manual world selection through old screen bounds.

Add sampled terrain-cut sides, mitered base trim/underside, infantry contrast
rings and depth-tested selected/hovered health bars. UI now has four tabs and
unit hover names; v4 layout stores optional tabletop startup and appearance
flags, migrating older data. Existing window tilt and normal fallback remain.

Native and both APK builds pass; 3258 host, 2278 Quest ARM64 and 65 actual GLES
checks pass. P9 installed with exact APK hash verification. Controller-required
Horizon dialog blocks launch; no new game process or worn-headset acceptance
is claimed. P8 APK and v3 layout retained in a temporary rollback directory.
Controls, evidence and physical acceptance checklist: PLAN-023 section 25.

## 2026-09-13: P8 workspace menu, expanded world graphics and overview

User accepts P7.4 reliable selection and authorizes all four graphics items.
Implement per-eye shadow/decal/stencil capture, head-centered world rendering
with expanded terrain/visibility, camera-space particle recovery, and spatial
attack feedback from actual engine messages. Preserve shroud and native game
rules. Prepare XR shadow resources before the mode switch; shadow fill cannot
modify MR coverage alpha. Large-scene visual/performance validation stays open.

Replace the permanent legend with a small UI button opening an upright
workspace menu, inspired by the read-only Tiberian Dawn Android reference.
Size/position/tilt/yaw/reset and map coverage are adjustable without memorizing
controller chords. Physical board maximum becomes 4 m, world coverage has
separate zoom, and v3 layout data persists both while migrating older files.
Build hover cards use localized labels of validated visible command gadgets,
including disabled buttons. Tabletop remains opt-in pending its later default.

Native and both APK builds pass, as do 1105 UBSan host checks, 438 Quest ARM64
checks, and 48 actual Adreno GLES checks. Standalone Canvas `app_process`
fixture was killed and is not counted as passing. P8 in-app visual acceptance
is separate. Accepted P7.4 APK and v2 layout are retained in a temporary local
verification directory. Details and headset checklist: PLAN-023 section 24.
P8 is installed with exact device APK hash verification and valid v2 signing.
The startup request is currently held by Horizon's controller-required dialog,
so no successful P8 in-game launch or visual acceptance is claimed yet.

## 2026-09-13: P7.4 visible controller ray, terrain collision and HQ eyes

User could not aim at the bulldozer in P7.3: a hit-only ring is not an
aiming affordance. Added an always-visible right-controller ray in play,
gray on misses, cyan on targets, orange when pressing, with a target ring
over the UI/world. Shared legend render/hit pose blocks click-through.
The ambiguous clipped popup report is explicitly deferred by the user.

Found a second, independent blocker: BaseHeightMap::Cast_Ray discarded
segments whose start or end was inside the broad terrain bounding box.
Preserve those endpoints and calculate cell bounds before the early exit.
A fixture compiles the actual production method verbatim with real WWMath:
the old function fails start-z=75/end-z=-50 over z=40 ground; the fix passes
all seven cases on Quest ARM64. Normal outside-camera rays and true misses
are included. Runtime read-only probes exercise the loaded map separately.

Stereo eye width increases from 1024 to 1536, maintaining runtime aspect
with a 2048 maximum dimension. Native ARM64 and both APK flavors build;
988 UBSan host checks plus 231 ARM64 projection/input checks pass. P7.4 is
installed with exact hash verification. Worn-headset selection/build and
HQ performance/clarity remain acceptance gates, not certified by fixtures.
Live match logs subsequently confirm 1536x1609 eye allocation, high/low
loaded-map probe hits, numerous controller hits and spatial commit dispatches.
Sampled counters report about 90 fps; no full unit/build acceptance is inferred.
User subsequently reports "Es laeuft hervorragend!" from the headset. Record
P7.4 as the accepted working baseline; preserve its interaction behavior while
continuing graphics work. Detailed full construction/production gates remain
separate from this positive overall acceptance.

## 2026-09-13: P7.3 spatial gameplay candidate and wider overview

User positively accepted P7.2 and authorized the playable tabletop step.
Expanded central framing from 65% to 80%, leaving physical board scale and
camera favorites independent. Added bounded inverse-board ray picking against
terrain/models, pointer-specific W3D ray overrides and cache isolation, plus
a room-space target ring. Conventional UI has first refusal; release-only
world gestures cancel on UI crossing, tracking loss or camera movement.

World trigger uses existing native context rules; armed/build commits use the
original GUI/placement translators. Spatial aim feeds their existing preview
path. Right grip cancels/deselects; normal planar input and camera navigation
remain available. Native ARM64 and both APK flavors build; 965 UBSan host
checks pass, including the production input router. Another 208 picking/input
checks pass on Quest ARM64. These tests do not certify real-game collision or
the full bulldozer/move/build/production flow. That headset gate remains open;
PLAN-023 records boundaries and artifact hashes.
P7.3 installed with exact hash verification; process 9792 entered an offline
match and stereo mode, showing a 619.7-unit span on a 1.31m board. The user was
given the trigger/context/build test sequence; successful commands and the
complete construction cycle are not yet confirmed.

## 2026-09-13: P7.2 missing models and stereo camera navigation

User sees real desert terrain in P7.1, but no models, apparent excessive zoom
and no scrolling. Pixel probes confirm visible RGBA and the live RGB-only
terrain mask. Found that the P7 filter rejected normal building/unit passes
using stencil ALWAYS to write occlusion tags. Allow these color/depth draws
with stencil disabled only in the independent eye targets; restore original
state afterward. Restrictive stencil and shadow passes stay excluded.

Added native, camera-only left-stick pan and right-stick zoom independent of
pointer hits, retaining script/modal/focus/arrangement gates. Unit orders stay
disabled. Kept scene scale unchanged until actual models can be assessed;
added world-span and terrain/model-count diagnostics. Native ARM64 and all
830 UBSan host checks pass, including new filter and navigation regressions.
Headset model visibility, navigation and perceived scale remain open.
Both APK flavors packaged; P7.2 installed successfully on Quest 3 with exact
installed hash verification. Process 3736 entered the shell and rendered two
valid XR views. Artifact hashes and acceptance boundaries are in PLAN-023.

## 2026-09-13: P7.1 Quest GPU regression and verified installation

After the Quest reconnected, all 35 expanded standalone GLES checks passed
on Adreno 740, including alpha invisibility reproduction and coverage repair.
Installed P7.1 successfully and verified the exact installed APK hash against
the build. XrHelloActivity resumed; process 29100 renders two valid XR views.
The user was asked to test left trigger + Y inside an offline match. Actual
game-world visibility and measured live-engine alpha remain open: controlled
GPU success and app startup alone do not close the reported gameplay defect.
PLAN-023 records artifact hashes and the deployment evidence.

## 2026-09-13: P7.1 invisible capture diagnosis and alpha/fallback candidate

User confirms P6 flutter is gone. Correct P7 activation then hides the board;
Quest logs confirm the switch and 80 repeated source draws, so input was not
the failure. The old draw-count readiness check could publish invisible eye
textures. Added XR-only alpha coverage for opaque materials and RGB-only
masks, preserving cutouts, blended alpha and the ordinary draw. Added initial
per-eye alpha/RGB readback and planar fallback for empty captures, with
bounded retry and no steady-state readback after a successful probe.

P7.1 labels now distinguish the actual-world path from `P6 TEST DIORAMA`.
Native ARM64 build and 760 existing host checks pass. Expanded GPU regression
compiles but device push failed after the Quest disconnected. Alpha is a
concrete code-level defect hypothesis; actual game pixel evidence and headset
confirmation remain pending, and no replacement installation is claimed.
See PLAN-023 for probe costs, limits and the next device check.
Both Android and XR APK packages subsequently built successfully; hashes are
recorded in the plan. They are not installed or device-validated yet.

## 2026-09-13: P7.0 real-geometry stereo and P6 coplanar-surface fix

User reports the P6 test worked and authorizes P7; additionally reports black
flutter. Found dark plinth and sand top faces coplanar at Z=0. Lowered the
plinth top by 0.005 board widths and added a regression; headset confirmation
of this specific fix remains open.

Added an offline, opt-in, view-only terrain/model stereo path. The engine
traverses the world once; eligible uploaded GPU draws execute immediately for
both eyes as well as the original fallback. This avoids duplicated scene and
animation callbacks. Current OpenXR poses/FOV drive projection and compositor
submission together. Uniform board mapping and fragment volume clipping use
shared, tested contracts. Sorted effects, stencil shadows, stereo-aware
culling and world picking are deliberately deferred; UI remains conventional.
Left trigger + Y in an offline split-view game toggles the prototype. The
simulation continues; pointer commands are disabled until returning to planar.

Native ARM64 and both APK flavors built; 760 UBSan host checks and 26 isolated
Quest 3 GLES checks passed. Visually inspected the test stereo pair. Installed
XR APK and verified its exact hash, recorded in PLAN-023. Launch currently
waits at the system Touch-controller-required dialog. Real-game stereo output,
performance, comfort and remaining flutter require worn-headset validation.
No P7 completion or release claim; P5 checks stay deferred at user request.

## 2026-09-13: P6 stereo miniature proof and free window tilt

User accepted P5.1's visual improvements and explicitly deferred the remaining
P5 completion checks to prioritize tabletop graphics. Added a shell-only,
opt-in stereo miniature scene: plinth, buildings, tanks, bridge, ridge, trees
and scaled 2/5/10cm calibration bars. A static 1,304-triangle mesh uses actual
per-eye OpenXR poses/FOV and uniformly scaled board-local height. Left trigger
+ Y opens the demo from the main menu; B returns. Arrangement still works;
demo controls do not reach game commands, and live matches cannot be hidden.
This is not yet stereo rendering of the actual Generals world (P7).

Fixed the reported command-window snap: manual window grabs override legacy
LEVEL flags and preserve their released angle. Y can explicitly align again;
automatic release snapping remains board-only. Poses, sizes and camera
favorites are preserved, with unchanged layout format.

Native ARM64 and both APK flavors built. 732 host checks passed with UBSan.
The production P6 mesh/shaders passed 23 standalone checks on Quest 3 Adreno
740, including eye-image and depth-occlusion differences. Visually inspected
the GPU-rendered stereo pair. Actual headset depth, comfort, window-tilt
persistence and P7 game-world integration remain open. Detailed controls,
hashes, test commands and the revised priorities are recorded in PLAN-023.
P6 installed successfully on Quest 3, with installed hash verified. Automatic
launch waits at the system controller-required dialog; activate the Touch
controllers before testing the in-app demo and window tilt.

## 2026-09-13: P5.1 color correction and compact command console

User confirmed the P5 split and reported washed-out desert maps, excess grey
UI area and flat/unreadable control help. Corrected display-encoded RGBA8 to
linear conversion in the XR host. The standalone Quest 3 GPU test reproduced
grey 128 becoming approximately 188 in the old sRGB eye target; the shared
production decoder preserves approximately 128. No engine lighting, Android
2D gamma, camera preset or saved favorite was changed.

The lower opaque command band now follows live control-bar geometry; the
remaining upper UI is transparent and unframed, retaining HUD and popovers.
Render and input share continuous crop/placement mapping; live GUI hit tests
let empty upper regions pass through to the board. Added a 55-degree legend
hinge for flat boards, relaxing toward zero on upright panels. Selected UI
help sits below the console to avoid upper popovers.

Native ARM64 and both APK flavors built successfully. All 685 host checks
passed with UBSan, and 72 isolated GLES checks passed on Quest 3 Adreno 740.
P5.1 replacement installation succeeded on Quest 3 `2G0YC5ZG9609PY`; artifact
hashes and reproducible tests are in PLAN-023. Full-game color/infantry,
compact UI interaction and physical legend readability remain headset gates,
alongside the inherited touch-preview and performance checks. Still a
monoscopic tabletop image, not stereoscopically raised geometry.

## 2026-09-13: P5 separate battlefield and UI surfaces

Implemented GPU-only world snapshot and MRT UI capture at the audited W3D UI
boundary. UI callbacks and engine frames still execute once; the composed RGB
frame remains the shell/modal/manual fallback. Added tactical-viewport crop,
independent whole-canvas command panel, depth-tested panel composition, routed
world/windows pointer input with delayed release handoff, and v1-to-v2 layout
migration preserving old board/screen poses and camera favorites.

Native ARM64 and both Android APK flavors built. 337 host checks passed with
UBSan, and a standalone GLES3 test passed 32 checks on the actual Quest 3 GPU.
The first live match remained COMBINED: hidden shell layouts remain stacked,
so checking stack count was wrong. Corrected the policy to shell activity,
rebuilt both flavors and installed the corrected APK. PLAN-023 records hashes,
controls and the physical acceptance checklist; no worn-headset pass inferred
from successful compilation or the isolated GPU test. Also identified the
inherited touch-only building/radius preview path as an explicit follow-up gate.
This is still a monoscopic world image, not raised stereoscopic miniatures.

## 2026-09-13: P4 tabletop perspective refinement and camera limit

The user positively accepted P3 and asked for a suitable standard perspective.
Device logs independently confirm match entry, all three old camera presets,
board movement and scaling. Source review confirms that a camera-only change
can improve readability but cannot turn the shared texture into raised stereo
miniatures. Moving 65 to 78 degrees changes local ground-axis projection scale
by only about 8 percent. PLAN-023 now explains this limit and recommends
world/UI separation followed by an eye-relative stereo proof for a diorama.

Implemented P4: candidate default 78 degrees, old 65/85/classic comparisons,
actual preset zoom reset, a saved personal yaw/pitch/desired-height default,
and visible degree/default feedback. L-trigger + R-stick click saves;
L-trigger + X restores. No board layout or engine game-rule changes.
Found that W3DView::setZoomToDefault only invalidates state, and that changing
the FOV field alone is not sufficient in the inspected release path; the
first is handled in the XR bridge, the second is documented rather than
bundled into this A/B. Native build, both APK flavors and 269 host checks pass.
P4 installed on Quest 3; automatic launch again waits for active controllers.
Worn-headset P4 perspective, save/restore and relaunch verification remain open.

## 2026-09-13: QTR-P3 native camera and saved board arrangement

Implemented the authorized camera-first slice in the existing native OpenXR
host: classic/65-degree/85-degree camera presets, yaw and modifier-based tilt,
plus exclusive arrangement mode for one-hand rigid grabbing and two-hand
uniform scaling. Controller transitions rebase without jumping; focus/tracking
loss requires release before rearming. Added physical size bounds, level/free
snap, reset/recenter, one-controller stick scaling, and separate upright/table
layouts persisted relative to launch heading. A small attached P3 legend makes
the new controls and mode visible. Original UI remains part of the same texture.

JNI SDL event initialization now declares the handled main entry correctly;
transient Activity pause no longer unconditionally destroys the native loop.
Both changes still need real lifecycle verification. Native ARM64 and both
Android APK flavors built successfully; 218 projection, placement, persistence
and mocked interaction checks passed. Placement tests also passed UBSan.
ASan stalled in its macOS allocator initialization before the test entry point;
sampled and terminated those test processes rather than claiming a pass.

The XR APK was installed successfully on Quest 3 `2G0YC5ZG9609PY`. Automatic
launch currently waits at the Quest controller-required system dialog. The user
was asked to activate both Touch controllers and verify the P3 legend and
manipulation. No new physical gameplay or lifecycle acceptance is claimed.
APK hashes, controls, reproducible tests and remaining gates are in PLAN-023.

## 2026-09-13: camera-first tabletop appearance and free arrangement

After accepting the corrected XR appearance, the user proposed the existing
game camera as a cheaper route to the desired depth impression. Source review
confirmed user-controlled yaw, pitch and zoom, including camera-constraint
updates in W3DView. PLAN-023 now prioritizes a reversible 2.5D camera comparison
before the optional true-stereo milestone.

The user also made free board/window movement and scaling a requirement.
The plan now covers manipulation capture, independent/grouped surfaces,
uniform scale, horizontal snap, camera-zoom separation, persisted relative
layouts, and hit testing after transforms. Added QTR-017 through QTR-019;
these are requirements, not features already shipped in QTR-P2.

## 2026-09-13: Quest projection and controller recovery

The screen that appeared far away and stretched during head yaw was caused by
an erroneous near-plane factor in the XR projection scale. Corrected that
formula and extracted the production transforms into a host-testable header.
All 111 numerical checks and the native ARM64 build passed.

The user confirmed the corrected appearance on Quest. Device evidence shows
controller input, match entry, recentering and real-room passthrough. Restoring
the old projection in a temporary test copy made the first assertion fail.

Added Quest Touch action bindings, a persistent ray cursor, click/drag,
secondary click, back, recenter, and camera sticks. The Android mouse stream
was disabled for touch; the XR path now enables it for its persistent pointer.
Y temporarily switches the whole in-game image between tabletop and upright
for access to existing menus. Separate world/UI layers remain planned.
The final safeguard disables XR edge scrolling and cancels grip scrolling
when the pointer is lost or released. The final APK packages successfully;
this safeguard still needs its own device behavior check.

Detailed evidence and remaining worn-headset gates are recorded in
`docs/WORKDIR/planning/PLAN-023_QUEST_TABLETOP_RECOVERY.md`.
